netstat is remplaced by ss
ss -nltp
lsof -i:PORT
ps -faux = procesos del sistema
ps -eo command = comandos especificos del sistema

# Cursor Blink
tput civis = ocultar el cursor
tput cnorm = recuperar el cursor

# SO
cat /etc/os-release = para saber el SO
